package main.counter;

/**
 * Created by marcus on 19.04.16.
 */
public class NeighborCounter {

    public static int countNeighbors() {
        int neighborCount = 0;
//        Node currentNode = startNode;
//
//        do {
//            int currentX = currentNode.getPosition().getX();
//            int currentY = currentNode.getPosition().getY();
//
//            int neighborX = currentX++;
//            int neighborY = currentY++;
//            neighborCount = addEnergyToTotalEnergy(nodes[neighborX][neighborY], neighborCount, currentNode);
//
//            neighborX = currentX--;
//            neighborCount = addEnergyToTotalEnergy(nodes[neighborX][neighborY], neighborCount, currentNode);
//
//            neighborY = currentY--;
//            neighborCount = addEnergyToTotalEnergy(nodes[neighborX][neighborY], neighborCount, currentNode);
//
//            neighborY = currentX++;
//            neighborCount = addEnergyToTotalEnergy(nodes[neighborX][neighborY], neighborCount, currentNode);
//
//            currentNode = currentNode.getNext();
//        } while (currentNode != null);

        return neighborCount;
    }
}
